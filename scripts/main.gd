# Main — Entry point scene script
# Loads the game scene; could later host a main menu.
extends Node

func _ready() -> void:
	# Everything is in the game scene; main just ensures autoloads are ready.
	pass
